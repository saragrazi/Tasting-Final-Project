// Shared max-width for the site's data-entry forms (recipe, sign-up, profile,
// contact), so they read as one consistent width instead of each picking its
// own number. Content/display pages (e.g. the recipe details page) are a
// different kind of layout and intentionally aren't tied to this.
export const FORM_MAX_WIDTH = 700;
